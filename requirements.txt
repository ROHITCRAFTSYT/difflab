# Core runtime dependencies (CPU-friendly; see requirements-gpu.txt for CUDA wheels).
# Install the package itself with `pip install -e ".[dev]"` for development.
torch>=2.1
diffusers>=0.27
transformers>=4.38
accelerate>=0.27
datasets>=2.18
huggingface-hub>=0.22
numpy>=1.24
Pillow>=9.5
PyYAML>=6.0
tqdm>=4.65
tensorboard>=2.14

# Audio pillar
torchaudio>=2.1
librosa>=0.10
soundfile>=0.12
